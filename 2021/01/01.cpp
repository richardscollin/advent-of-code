#include <iostream>
#include <string>

int main()
{
    std::string s;

    int line_count = 0;
    while (std::getline(std::cin, s)) {
    }

    std::cout << line_count << std::endl;
    return 0;
}
